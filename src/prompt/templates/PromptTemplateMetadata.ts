/**
 * Metadata for `PromptTemplate` (source, license, etc.).
 */
export interface PromptTemplateMetadata {
  source?: string
  createdAt?: string
  updatedAt?: string
  repository?: string
  homepage?: string
  license?: string
}
